interface:
  display_name: "Dynamic Workflow"
  short_description: "Orchestrate large tasks with isolated Codex workers"
  default_prompt: "Use $dynamic-workflow to plan and run this task as a verified multi-agent workflow."
